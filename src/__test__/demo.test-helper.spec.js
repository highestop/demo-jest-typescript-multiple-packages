"use strict";
describe('[index]', () => {
    describe('given/arrange', () => {
        describe('when/act', () => {
            beforeEach(() => { });
            afterEach(() => { });
            test('then/assert', () => { });
        });
    });
});
